import { styled } from 'styled-components';

export const SectionAnimes = styled.section`
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            align-items: center;
            background-color:#1A1A1A ;
            height: 100%;

            a{
                width: 240px;
            }

`;